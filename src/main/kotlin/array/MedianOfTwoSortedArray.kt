package array

/*
fun findMedianSortedArrays(nums1: IntArray, nums2: IntArray): Double {
    var index1 = 0
    var index2 = 0
    var mainIndex = 0
    var isEvenSizedList = false
    var m1 = -1
    var m2 = -1
    val totalSize = nums1.size + nums2.size
    if ((totalSize) % 2 == 0) {
        isEvenSizedList = true
    }

    while (index1 < nums1.size || index2 < nums2.size) {
        if(nums1[index1]<nums2[index2]){

        }

        if (isEvenSizedList && mainIndex == ((totalSize - 1) / 2)) {

        }
        mainIndex++
    }
}*/
